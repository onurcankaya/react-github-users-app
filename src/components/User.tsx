import styled from 'styled-components'

import { Card } from '../components'

export const User = () => {
  return (
    <Wrapper>
      <Card />
    </Wrapper>
  )
}

const Wrapper = styled.div`
  padding-bottom: 2rem;
`
